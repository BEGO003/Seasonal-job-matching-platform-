package grad_project.seasonal_job_matching.seasonal_job_matching.model;
//In case we turn the jobseeker into User since jobseeker and employer have the same profile
public enum Type {
    EMPLOYER, 
    JOB_SEEKER
}
